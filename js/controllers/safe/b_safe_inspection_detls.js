define('controllers/safe/b_safe_inspection_detls', function(require, exports, module) {
    "use strict";
    require('mui_zoom');
    require('mui_previewimage');
    
    var fnGlobalUrl = require[require.toUrl ? 'toUrl' : 'resolve'];
    
    /* ************************************************************ */
    exports.run = function() {
		mui.previewImage();
		
		
		
    };
});